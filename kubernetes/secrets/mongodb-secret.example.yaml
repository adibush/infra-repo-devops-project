# Example only - do not store real credentials in Git
apiVersion: v1
kind: Secret

metadata:
  name: mongodb-secret
  namespace: hotel-project

type: Opaque

stringData:
  MONGO_INITDB_ROOT_USERNAME: YOUR_MONGO_USERNAME
  MONGO_INITDB_ROOT_PASSWORD: YOUR_MONGO_PASSWORD
