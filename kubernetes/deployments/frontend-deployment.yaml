apiVersion: apps/v1
kind: Deployment

metadata:
  name: frontend-deployment
  namespace: hotel-project

spec:
  replicas: 5

  selector:
    matchLabels:
      app: frontend

  template:
    metadata:
      labels:
        app: frontend

    spec:
      containers:
        - name: frontend

          image: adibush/hotel-frontend:d97cf36b5f49cdc8808783b8deb0888022d82bc9

          imagePullPolicy: Always

          ports:
            - containerPort: 80
